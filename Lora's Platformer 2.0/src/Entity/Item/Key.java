package Entity.Item;

import Entity.MapObject;
import TileMap.TileMap;

public class Key extends Item
{

	public Key(TileMap tileMap, boolean inWorld, double locationX, double locationY, MapObject owner) {
		super(tileMap, inWorld, locationX, locationY, owner);

	}

}
